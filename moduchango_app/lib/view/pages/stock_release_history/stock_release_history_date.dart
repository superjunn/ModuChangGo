import 'package:flutter/material.dart';

class StockReleaseHistoryDate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Date"),
    );
  }
}
