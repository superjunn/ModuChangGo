import 'package:flutter/material.dart';

class Body1Page extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("Body1 Page"),
      ),
    );
  }
}
